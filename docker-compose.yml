services:
  db:
    image: postgres:16
    container_name: labo04_db
    environment:
      POSTGRES_DB: labo04
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: